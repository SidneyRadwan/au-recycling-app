spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/recycling
    username: recycling
    password: recycling_dev
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.australiarecycling: DEBUG
    org.springframework.web: DEBUG
