spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://db:5432/recycling}
    username: ${DATABASE_USERNAME:recycling}
    password: ${DATABASE_PASSWORD:recycling_dev}
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.australiarecycling: DEBUG
    org.springframework.web: DEBUG
